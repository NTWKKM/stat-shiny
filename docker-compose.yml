services:
  app:
    build: .
    ports:
      - "7860:7860"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
    command: python -m shiny run app.py --host 0.0.0.0 --port 7860 --reload
