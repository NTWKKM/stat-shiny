services:
  app:
    build: .
    user: "${UID:-1000}:${GID:-1000}"
    ports:
      - "7860:7860"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
    command: python -m shiny run app.py --host 0.0.0.0 --port 7860 --reload
